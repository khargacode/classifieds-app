# Classifieds App (Full Scaffold)

Stack:
- Next.js (App Router) + TypeScript
- MongoDB (Mongoose)
- NextAuth (NextAuth.js)
- Cloudinary (image hosting)
- SWR for client data fetching

How to run locally:
1. Clone or create the project folder `classifieds-app/`.
2. Copy files from the scaffold into the matching paths.
3. Copy `.env.example` → `.env.local` and fill the variables.
4. Install deps:
   ```bash
   npm install
